## This file is meant for the quick creation of a new cuda ready environment

name: test

channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - cudatoolkit=11.3
  - mpi4py=3.0.3 # installs cuda-aware openmpi
  - pip=20.0
  - python=3.7.13
  - pytorch=1.12.1
  - torchvision=0.13.1

  #conda install -n ipykernel --update-deps --force-reinstall